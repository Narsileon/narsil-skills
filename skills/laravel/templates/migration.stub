<?php

#region USE

use App\Models\Post;
use App\Models\User;
use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

#endregion

return new class extends Migration
{
    #region PUBLIC METHODS

    /**
     * @return void
     */
    public function down(): void
    {
        Schema::dropIfExists(Post::TABLE);
    }

    /**
     * @return void
     */
    public function up(): void
    {
        if (!Schema::hasTable(Post::TABLE))
        {
            $this->createPostsTable();
        }
    }

    #endregion

    #region PRIVATE METHODS

    /**
     * @return void
     */
    private function createPostsTable(): void
    {
        Schema::create(Post::TABLE, function (Blueprint $blueprint)
        {
            $blueprint
                ->id(Post::ID);
            $blueprint
                ->string(Post::TITLE);
            $blueprint
                ->text(Post::BODY)
                ->nullable();
            $blueprint
                ->foreignId(Post::USER_ID)
                ->constrained(User::TABLE);
            $blueprint
                ->timestamps();
        });
    }

    #endregion
};
